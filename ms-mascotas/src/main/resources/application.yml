spring:
  config:
    import: optional:configserver:http://localhost:8888/
  application:
    name: ms-mascotas
  profiles:
    active: dev
springdoc:
  enable-hateoas: false
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
