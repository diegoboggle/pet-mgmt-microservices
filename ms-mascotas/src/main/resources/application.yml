spring:
  application:
    name: ms-mascotas
  profiles:
    active: dev # Perfil por defecto

# Aplicamos Swagger OpenAPI (Tus rutas antiguas)
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html